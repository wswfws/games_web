diff --git a/node_modules/@originjs/vite-plugin-federation/dist/index.js b/node_modules/@originjs/vite-plugin-federation/dist/index.js
index 13056b1..cad789a 100644
--- a/node_modules/@originjs/vite-plugin-federation/dist/index.js
+++ b/node_modules/@originjs/vite-plugin-federation/dist/index.js
@@ -1261,7 +1261,7 @@ function prodExposePlugin(options) {
           return res;
         }, /* @__PURE__ */ new Map());
         remoteEntryChunk.code = remoteEntryChunk.code.replace(
-          new RegExp(`(["'])${DYNAMIC_LOADING_CSS_PREFIX}.*?\\1`, "g"),
+          new RegExp(`(["'\`])${DYNAMIC_LOADING_CSS_PREFIX}.*?\\1`, "g"),
           (str) => {
             if (viteConfigResolved.config && !viteConfigResolved.config.build.cssCodeSplit) {
               if (cssBundlesMap.size) {
@@ -1273,7 +1273,7 @@ function prodExposePlugin(options) {
               }
             }
             const filepath = str.slice(
-              (`'` + DYNAMIC_LOADING_CSS_PREFIX).length,
+              (str[0] + DYNAMIC_LOADING_CSS_PREFIX).length,
               -1
             );
             if (!filepath || !filepath.length)
@@ -1344,10 +1344,11 @@ function prodExposePlugin(options) {
         const magicString = new MagicString(remoteEntryChunk.code);
         walk(ast, {
           enter(node) {
-            var _a2, _b2;
-            if (node && node.type === "CallExpression" && typeof ((_a2 = node.arguments[0]) == null ? void 0 : _a2.value) === "string" && ((_b2 = node.arguments[0]) == null ? void 0 : _b2.value.indexOf(
+            const _arg0 = node && node.arguments ? node.arguments[0] : void 0;
+            const _argStr = _arg0 ? typeof _arg0.value === "string" ? _arg0.value : _arg0.quasis && _arg0.quasis[0] && _arg0.quasis[0].value && _arg0.quasis[0].value.raw : void 0;
+            if (node && node.type === "CallExpression" && typeof _argStr === "string" && _argStr.indexOf(
               `${DYNAMIC_LOADING_CSS_PREFIX}`
-            )) > -1) {
+            ) > -1) {
               magicString.remove(node.start, node.end + 1);
             }
           }
diff --git a/node_modules/@originjs/vite-plugin-federation/dist/index.mjs b/node_modules/@originjs/vite-plugin-federation/dist/index.mjs
index 960932e..a9d9b4f 100644
--- a/node_modules/@originjs/vite-plugin-federation/dist/index.mjs
+++ b/node_modules/@originjs/vite-plugin-federation/dist/index.mjs
@@ -1244,7 +1244,7 @@ function prodExposePlugin(options) {
           return res;
         }, /* @__PURE__ */ new Map());
         remoteEntryChunk.code = remoteEntryChunk.code.replace(
-          new RegExp(`(["'])${DYNAMIC_LOADING_CSS_PREFIX}.*?\\1`, "g"),
+          new RegExp(`(["'\`])${DYNAMIC_LOADING_CSS_PREFIX}.*?\\1`, "g"),
           (str) => {
             if (viteConfigResolved.config && !viteConfigResolved.config.build.cssCodeSplit) {
               if (cssBundlesMap.size) {
@@ -1256,7 +1256,7 @@ function prodExposePlugin(options) {
               }
             }
             const filepath = str.slice(
-              (`'` + DYNAMIC_LOADING_CSS_PREFIX).length,
+              (str[0] + DYNAMIC_LOADING_CSS_PREFIX).length,
               -1
             );
             if (!filepath || !filepath.length)
@@ -1327,10 +1327,11 @@ function prodExposePlugin(options) {
         const magicString = new MagicString(remoteEntryChunk.code);
         walk(ast, {
           enter(node) {
-            var _a2, _b2;
-            if (node && node.type === "CallExpression" && typeof ((_a2 = node.arguments[0]) == null ? void 0 : _a2.value) === "string" && ((_b2 = node.arguments[0]) == null ? void 0 : _b2.value.indexOf(
+            const _arg0 = node && node.arguments ? node.arguments[0] : void 0;
+            const _argStr = _arg0 ? typeof _arg0.value === "string" ? _arg0.value : _arg0.quasis && _arg0.quasis[0] && _arg0.quasis[0].value && _arg0.quasis[0].value.raw : void 0;
+            if (node && node.type === "CallExpression" && typeof _argStr === "string" && _argStr.indexOf(
               `${DYNAMIC_LOADING_CSS_PREFIX}`
-            )) > -1) {
+            ) > -1) {
               magicString.remove(node.start, node.end + 1);
             }
           }
